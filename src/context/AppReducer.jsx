
export default (state, action) => {
    switch (action.type) {
        case "ADD_TRANSACTION":
            return state
            break;

        default:
            return state
            break;
    }
}